{
  "name": "bookmarklet-maker",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "lerna run build",
    "test": "lerna run test",
    "lint": "lerna run lint",
    "clean": "lerna run clean"
  },
  "devDependencies": {
    "lerna": "^8.1.2",
    "esbuild": "^0.20.1",
    "terser": "^5.27.0"
  }
}
